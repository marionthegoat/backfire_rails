= BackfireRails

This project uses MIT-LICENSE.

Tinkering Notes :

o You can run the test app interactively by running webrick from the /test/dummy directory
o You will need to have the Backfire gem pulled from github and installed locally ( gemfile may require some doinking )

