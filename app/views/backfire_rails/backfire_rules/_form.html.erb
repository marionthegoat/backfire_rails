<%= form_for([@backfire_control,@backfire_rule]) do |f| %>
  <% if @backfire_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@backfire_rule.errors.count, "error") %> prohibited this backfire_rule from being saved:</h2>

      <ul>
      <% @backfire_rule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
  <div class="field span4">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field span 8">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  </div>
    <div class="field">
      <%= f.label "Receiving Fact" %><br />
      <%= f.text_field :fact %>
    </div>
  <div class="field">
    <%= f.label :assertion %><br />
    <%= f.text_area :assertion %>
  </div>
  <div class="field">
    <%= f.label :predicate %><br />
    <%= f.text_area :predicate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
